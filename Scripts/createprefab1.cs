using UnityEngine;
using System.Collections;

public class createprefab1 : MonoBehaviour {
public Transform BasicRoom;

void Start() {
	for (int y = 0; y < 1; y++) {
		for (int x = 0; x < 1; x++) {
			//Instantiate(BasicRoom, new Vector3(x, y, 0), Quaternion.identity);
		}
	}
}


	
	// Update is called once per frame
	void Update () {
	
	}
}
